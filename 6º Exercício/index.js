const usuario = 'Iarlyson';
const idade = 23;
console.log(usuario)
const mensagem = `o usuario ${usuario}
possui ${idade} anos`;

function printUser (): string {
    const usuario = 'Iarlyson';
    const idade = 23;
    return  `o usuario ${usuario}
    possui ${idade} anos`; 
}

console.log(printUser)